{# Sidebar desktop (md+) — le menu mobile est dans dashboard (offcanvas) #}

<aside class="col-12 col-md-3 col-lg-2 border-end d-none d-md-flex flex-column dashboard-sidebar-desktop">

    <div class="p-4 border-bottom text-center flex-shrink-0">
        <a href="{{ path('app_home') }}" class="text-decoration-none d-inline-block">
            <img src="{{ asset('logo-souk-expat-nav.png') }}" alt="SoukExpat" width="140" height="43" style="width: 140px; height: auto; object-fit: contain;" decoding="async">
            <p class="small text-muted mb-0 mt-1 font-monospace" style="font-size: 0.7rem;">ADMINISTRATION</p>
        </a>
    </div>

    <div class="flex-grow-1 overflow-y-auto p-3 custom-scrollbar min-h-0">
        {{ include('admin/_sidebar_nav_content.html.twig') }}
    </div>

    <div class="dashboard-sidebar-footer p-3 border-top flex-shrink-0">
        <div class="d-flex align-items-center justify-content-between gap-2 mb-2">
            <span class="small text-muted fw-semibold">Thème</span>
            {{ include('layouts/_theme_toggle.html.twig', { btn_class: 'theme-toggle--admin' }) }}
        </div>
        {{ include('security/_logout_button.html.twig', {
            variant: 'sidebar-footer',
            form_class: 'd-block m-0'
        }) }}
    </div>
</aside>
