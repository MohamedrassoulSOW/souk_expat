<footer class="footer-modern bg-dark text-white pt-5 mt-5" data-animate="fade-up">
    <div class="container">
        <div class="row g-4 mb-5">
            {# Colonne 1 : À propos #}
            <div class="col-12 col-lg-4 col-md-6 mb-4 mb-md-0">
                <a class="navbar-brand fw-bold text-white fs-3 mb-3 d-flex align-items-center justify-content-center justify-content-md-start gap-3 text-decoration-none text-center text-md-start" href="{{ path('app_home') }}">
                    <span class="footer-brand-logo rounded-circle overflow-hidden border flex-shrink-0">
                        <img src="{{ asset('logo-souk-expat.png') }}" alt="Expat Souk" width="52" height="52" loading="lazy" decoding="async">
                    </span>
                    <span class="text-white">Expat<span style="color: #42B1CE;">Souk</span></span>
                </a>
                <p class="text-secondary small pe-lg-5 text-center text-md-start">
                    La première plateforme dédiée aux expatriés au Maroc. Achetez, vendez et échangez en toute confiance au sein de votre communauté.
                </p>
                <div class="social-links d-flex gap-3 mt-4">
                    <a href="#" class="social-icon"><i class="bi bi-facebook"></i></a>
                    <a href="#" class="social-icon"><i class="bi bi-instagram"></i></a>
                    <a href="#" class="social-icon"><i class="bi bi-linkedin"></i></a>
                </div>
            </div>

            {# Colonne 2 : Liens rapides #}
            <div class="col-6 col-lg-2 col-md-6 mb-4 mb-md-0">
                <h5 class="fw-bold mb-4 text-center text-md-start">Navigation</h5>
                <ul class="list-unstyled footer-links">
                    <li><a href="{{ path('app_home') }}">Accueil</a></li>
                    <li><a href="{{ path('app_annonce_index') }}">Toutes les annonces</a></li>
                    <li><a href="{{ path('app_how_it_works') }}">Comment ça marche ?</a></li>
                    <li><a href="{{ path('app_blog') }}">Blog & Conseils</a></li>
                </ul>
            </div>

            {# Colonne 3 : Support #}
            <div class="col-6 col-lg-2 col-md-6 mb-4 mb-md-0">
                <h5 class="fw-bold mb-4 text-center text-md-start">Aide & Support</h5>
                <ul class="list-unstyled footer-links">
                    <li><a href="{{ path('app_contact') }}">Contactez-nous</a></li>
                    <li><a href="{{ path('app_about') }}">À propos de nous</a></li>
                    <li><a href="{{ path('app_faq') }}">FAQ</a></li>
                    <li><a href="{{ path('app_legal') }}">Mentions légales</a></li>
                </ul>
            </div>

            {# Colonne 4 : Newsletter #}
            <div class="col-12 col-lg-4 col-md-6">
                <h5 class="fw-bold mb-4 text-center text-md-start">Restez informé</h5>
                <p class="text-secondary small text-center text-md-start">Recevez les meilleures pépites du Souk chaque semaine.</p>
                <form class="mt-3">
                    <div class="input-group">
                        <input type="email" class="form-control bg-transparent border-secondary text-white shadow-none" placeholder="Votre email" aria-label="Email">
                        <button class="btn btn-custom-cyan px-3" type="button">
                            <i class="bi bi-send"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>

        {# Bas du footer #}
        <div class="footer-bottom py-4 border-top border-secondary border-opacity-25 text-center text-md-between d-md-flex align-items-center justify-content-between">
            <p class="text-secondary small mb-md-0">
                &copy; {{ "now"|date("Y") }} <strong>Expat-Souk</strong>. Tous droits réservés.
            </p>
            <p class="small mb-0">Développé avec ❤️ par <a href="#" style="text-decoration: none; color: #42B1CE; font-weight: 600;">Expat-Souk Team</a></p>
            <div class="payment-icons text-secondary opacity-50 d-none d-md-block">
                v.0.0.1
            </div>
        </div>
    </div>
</footer>

<style>
    .footer-modern {
        background-color: #1a1d20 !important;
        font-family: 'Inter', sans-serif;
    }

    .footer-brand-logo {
        width: 54px;
        height: 54px;
        border-width: 2px !important;
        border-color: rgba(66, 177, 206, 0.45) !important;
        background: rgba(255, 255, 255, 0.06);
        box-shadow: 0 4px 16px rgba(66, 177, 206, 0.15);
    }
    .footer-brand-logo img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: center;
    }

    /* Bouton personnalisé */
    .btn-custom-cyan {
        background-color: #42B1CE;
        border-color: #42B1CE;
        color: white;
    }
    .btn-custom-cyan:hover {
        background-color: #369ab3;
        border-color: #369ab3;
        color: white;
    }

    /* Style des liens */
    .footer-links li { margin-bottom: 12px; }
    .footer-links a {
        color: #adb5bd;
        text-decoration: none;
        transition: all 0.3s ease;
        font-size: 0.95rem;
    }
    .footer-links a:hover {
        color: #42B1CE;
        padding-left: 5px;
    }

    /* Icônes Sociales */
    .social-icon {
        width: 38px; height: 38px;
        background: rgba(255,255,255,0.05);
        color: white;
        display: flex; align-items: center; justify-content: center;
        border-radius: 10px; text-decoration: none;
        transition: all 0.3s ease;
    }
    .social-icon:hover {
        background: #42B1CE;
        color: white;
        transform: translateY(-3px);
    }

    /* Focus Newsletter */
    .footer-modern .form-control:focus {
        border-color: #42B1CE;
        box-shadow: none;
    }

    @media (max-width: 768px) {
        .footer-modern { text-align: center; }
        .social-links { justify-content: center; }
        .footer-links { 
            text-align: center;
        }
        .footer-links a:hover { padding-left: 0; }
        .footer-bottom { 
            flex-direction: column; 
            gap: 15px; 
            text-align: center;
        }
        .footer-links li {
            margin-bottom: 8px;
        }
    }
    
    @media (max-width: 576px) {
        .footer-modern {
            padding-top: 3rem !important;
        }
        .navbar-brand {
            font-size: 1.5rem !important;
        }
    }
</style>