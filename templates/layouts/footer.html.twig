<footer class="footer-modern bg-dark text-white pt-5 mt-5">
    <div class="container">
        <div class="row g-4 mb-5">
            {# Colonne 1 : À propos #}
            <div class="col-lg-4 col-md-6">
                <a class="navbar-brand fw-bold text-white fs-3 mb-3 d-block" href="{{ path('app_home') }}">
                    <i class="bi bi-shop me-2" style="color: #42B1CE;"></i>Expat<span style="color: #42B1CE;">Souk</span>
                </a>
                <p class="text-secondary small pe-lg-5">
                    La première plateforme dédiée aux expatriés au Maroc. Achetez, vendez et échangez en toute confiance au sein de votre communauté.
                </p>
                <div class="social-links d-flex gap-3 mt-4">
                    <a href="#" class="social-icon"><i class="bi bi-facebook"></i></a>
                    <a href="#" class="social-icon"><i class="bi bi-instagram"></i></a>
                    <a href="#" class="social-icon"><i class="bi bi-linkedin"></i></a>
                </div>
            </div>

            {# Colonne 2 : Liens rapides #}
            <div class="col-lg-2 col-md-6">
                <h5 class="fw-bold mb-4">Navigation</h5>
                <ul class="list-unstyled footer-links">
                    <li><a href="{{ path('app_home') }}">Accueil</a></li>
                    <li><a href="{{ path('app_annonce_index') }}">Toutes les annonces</a></li>
                    <li><a href="#">Comment ça marche ?</a></li>
                    <li><a href="#">Blog & Conseils</a></li>
                </ul>
            </div>

            {# Colonne 3 : Support #}
            <div class="col-lg-2 col-md-6">
                <h5 class="fw-bold mb-4">Aide & Support</h5>
                <ul class="list-unstyled footer-links">
                    <li><a href="{{ path('app_contact') }}">Contactez-nous</a></li>
                    <li><a href="{{ path('app_about') }}">À propos de nous</a></li>
                    <li><a href="#">FAQ</a></li>
                    <li><a href="#">Mentions légales</a></li>
                </ul>
            </div>

            {# Colonne 4 : Newsletter #}
            <div class="col-lg-4 col-md-6">
                <h5 class="fw-bold mb-4">Restez informé</h5>
                <p class="text-secondary small">Recevez les meilleures pépites du Souk chaque semaine.</p>
                <form class="mt-3">
                    <div class="input-group">
                        <input type="email" class="form-control bg-transparent border-secondary text-white shadow-none" placeholder="Votre email" aria-label="Email">
                        <button class="btn btn-custom-cyan px-3" type="button">
                            <i class="bi bi-send"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>

        {# Bas du footer #}
        <div class="footer-bottom py-4 border-top border-secondary border-opacity-25 text-center text-md-between d-md-flex align-items-center justify-content-between">
            <p class="text-secondary small mb-md-0">
                &copy; {{ "now"|date("Y") }} <strong>Expat-Souk</strong>. Tous droits réservés.
            </p>
            <p class="small mb-0">Développé avec ❤️ par <a href="#" style="text-decoration: none; color: #42B1CE; font-weight: 600;">Expat-Souk Team</a></p>
            <div class="payment-icons text-secondary opacity-50 d-none d-md-block">
                <i class="bi bi-credit-card-2-back fs-4 mx-2"></i>
                <i class="bi bi-paypal fs-4 mx-2"></i>
                <i class="bi bi-shield-check fs-4 mx-2"></i>
            </div>
        </div>
    </div>
</footer>

<style>
    .footer-modern {
        background-color: #1a1d20 !important;
        font-family: 'Inter', sans-serif;
    }

    /* Bouton personnalisé */
    .btn-custom-cyan {
        background-color: #42B1CE;
        border-color: #42B1CE;
        color: white;
    }
    .btn-custom-cyan:hover {
        background-color: #369ab3;
        border-color: #369ab3;
        color: white;
    }

    /* Style des liens */
    .footer-links li { margin-bottom: 12px; }
    .footer-links a {
        color: #adb5bd;
        text-decoration: none;
        transition: all 0.3s ease;
        font-size: 0.95rem;
    }
    .footer-links a:hover {
        color: #42B1CE;
        padding-left: 5px;
    }

    /* Icônes Sociales */
    .social-icon {
        width: 38px; height: 38px;
        background: rgba(255,255,255,0.05);
        color: white;
        display: flex; align-items: center; justify-content: center;
        border-radius: 10px; text-decoration: none;
        transition: all 0.3s ease;
    }
    .social-icon:hover {
        background: #42B1CE;
        color: white;
        transform: translateY(-3px);
    }

    /* Focus Newsletter */
    .footer-modern .form-control:focus {
        border-color: #42B1CE;
        box-shadow: none;
    }

    @media (max-width: 768px) {
        .footer-modern { text-align: center; }
        .social-links { justify-content: center; }
        .footer-links a:hover { padding-left: 0; }
        .footer-bottom { flex-direction: column; gap: 15px; }
    }
</style>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">