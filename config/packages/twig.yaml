twig:
    file_name_pattern: '*.twig'
    globals:
        # contact_email + site : fournis par App\Twig\SiteExtension
        # Hub Mercure (temps réel chat) — même valeur que MERCURE_PUBLIC_URL côté navigateur
        mercure_hub_url: '%env(MERCURE_PUBLIC_URL)%'

when@test:
    twig:
        strict_variables: true
